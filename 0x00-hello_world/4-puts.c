#include <stdio.h>

/**
 * main - this is the main function
 * puts - prints out the string
 * the slash adds in the entire string
 *
 * Return: always 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
return (0);
}
